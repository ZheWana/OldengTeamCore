## 更新内容

- 修复配置导出错误包含本地诊断日志的问题。设置加载与导出现在都使用明确的字段白名单，诊断日志、运行缓存及未来新增的非配置字段不会进入共享字符串。
- 大幅缩短配置字符串。实际测试中，包含 800 条诊断日志的配置从约 29 KB 降至 688 个字符。
- 保持 `tc1.` 压缩格式以及现有导入流程不变；本机用户名和自动同步选择仍不会被共享配置覆盖。

## 兼容性

- 最低 Obsidian 版本：`1.12.3`
- 发布文件：`main.js`、`manifest.json`、`styles.css`
